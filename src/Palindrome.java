public class Palindrome {
    private string word;
    private boolean isPalindrome;

    public Palindrome(String wd)
    {
        this.word = wd;
    }

    public boolean testWord()
    {
        // logic to test Palindrome

        // Srt instance variable
        return isPalindrome;
    }
}
